[package]
name = "sanguine_wayland_capture"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { version = "1.0", features = ["full"] }
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
zbus = { version = "5.17.0", features = ["blocking"] }
pipewire = "0.10.0"
libspa = "0.10.0"
libc = "0.2"
